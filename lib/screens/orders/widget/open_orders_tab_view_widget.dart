import 'package:flutter/material.dart';

class OpenOrdersTabViewWidget extends StatelessWidget {
  const OpenOrdersTabViewWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
